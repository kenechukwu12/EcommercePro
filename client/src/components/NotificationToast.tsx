import { useToast } from "@/hooks/use-toast";

const NotificationToast = () => {
  // Not rendering anything here since we're using Shadcn's toast system
  // This component is just a placeholder for consistency with the design reference
  // All toast functionality is handled through the useToast hook
  
  return null;
};

export default NotificationToast;
